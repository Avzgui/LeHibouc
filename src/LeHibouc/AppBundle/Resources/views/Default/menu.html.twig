{# src/LeHibouc/AppBundle/Resources/views/Default/menu.html.twig #}

<h4> What would you like to do{% if is_granted("IS_AUTHENTICATED_REMEMBERED") %},
        {{ app.user.username }}
    {% endif %}
    ?
</h4>

<ul class="nav nav-pills nav-stacked">
    <li><a href="{{ path('app_homepage') }}">Home</a></li>
    <li><a href="{{ path('library_homepage') }}">Library</a></li>
    <li>{% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
            {% if is_granted("ROLE_ADMIN") %}
                <li><a href="{{ path('admin_users_list') }}">Users</a></li>
            {% else %}
                <li><a href="{{ path('fos_user_profile_show') }}">Profile</a></li>
            {% endif %}
            <li><a href="{{ path('fos_user_security_logout') }}">Logout</a></li>
        {% else %}
            <a href="{{ path('fos_user_security_login') }}">Login</a>
            <a href="{{ path('fos_user_registration_register') }}">Register</a>
        {% endif %}
    </li>
</ul>